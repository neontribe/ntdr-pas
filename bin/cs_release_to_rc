#!bin/bash

USAGE="$0 -s <LOCAL DRUPLA ROOT> -r <REMOTE HOST> -t <REMOTE DRUPAL ROOT> -l <LATEST DRUPAL ROOT>"

ansible-playbook push-local-to-remote.yml -i inventory/cottage-servers --limit $RH --extra-vars="source=$LDR target=$RDR"
